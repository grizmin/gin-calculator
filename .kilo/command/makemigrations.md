---
description: Generate migrations for model changes
---
Generate database migrations for the calculator app.

Run `pyenv activate gin` then `python manage.py makemigrations calculator`. Use after modifying `calculator/models.py`.
