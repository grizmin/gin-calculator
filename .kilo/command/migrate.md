---
description: Apply pending database migrations
---
Apply pending database migrations with `python manage.py migrate`.

Activate pyenv first with `pyenv activate gin`. Run after any `makemigrations` or model changes.
