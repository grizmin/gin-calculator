---
description: Run Django test suite
---
Run the Django test suite with `python manage.py test`.

Always activate pyenv with `pyenv activate gin` first. Run this before finishing any changes that touch models, views, or serializers.
