---
description: Seed the default Classic London Dry recipe
---
Seed the default "Classic London Dry" recipe into the database.

Activate pyenv with `pyenv activate gin` then run `python manage.py create_default_recipe`. Use when starting fresh or when the database lacks sample data.
