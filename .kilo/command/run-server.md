---
description: Run the Django development server
---
Run the Django development server.

Use `pyenv activate gin` before running. This starts the local dev server so you can test changes in the browser.
