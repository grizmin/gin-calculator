# Test Cleanup & Simplification

## Goal
Remove duplicate tests, fix brittle assertions, and keep only tests that catch real bugs
instead of tests that replicate implementation logic.

## Issues to Fix
1. **Duplicate test files**: `calculator/tests.py` is a subset of `tests/test_calculator_views.py`
2. **Brittle formula tests**: `test_abv_volume_derived_correctly` and `test_calculate_formula` test the same formula they implement
3. **Non-existent API field**: `water_for_maceration` assertion in API tests (field doesn't exist in response)
4. **Fixture content tests**: Will break whenever fixture data changes

## Files & Steps

### Step 1 — Delete duplicate test file
**File:** `calculator/tests.py`

**Change:** Delete the entire file (it's 100% duplicated by `tests/test_calculator_views.py`).

**Verify:** 
```bash
ls calculator/tests.py  # should not exist
git status
```

---

### Step 2 — Remove non-existent API field assertion
**File:** `tests/test_calculator_views.py`

**Change:** Remove line 67 and the corresponding assertion from `test_get_recipe_endpoint`:
```python
# DELETE THIS LINE:
self.assertEqual(recipe['water_for_maceration'], 0.0)
```

Also remove line 71 from the expected_fields list:
```python
# BEFORE:
expected_fields = ['id', 'name', 'description', 'image_url', 'base_volume', 
                  'abv_volume', 'target_abv_percentage', 'water_for_maceration', 'ingredients']

# AFTER:
expected_fields = ['id', 'name', 'description', 'image_url', 'base_volume', 
                  'abv_volume', 'target_abv_percentage', 'ingredients']
```

**Verify:**
```bash
python manage.py test tests.test_calculator_views::CalculatorViewsTest::test_get_recipe_endpoint
```

---

### Step 3 — Simplify brittle formula test
**File:** `tests/test_calculator_views.py`

**Change:** Rename and comment `test_abv_volume_derived_correctly` to clarify it's a fixture consistency check, not a formula verification:

```python
def test_fixture_abv_volume_is_self_consistent(self):
    """Verify fixture data is internally consistent: abv_volume matches base_volume * target_abv%.
    This tests the fixture, not the formula — if fixture data changes, update this test."""
    from django.core.management import call_command
    call_command('create_famous_recipes')
    
    recipes = GinRecipe.objects.filter(
        name__in=[
            'Tanqueray London Dry',
            "Hendrick's",
            'Bombay Sapphire',
            'Beefeater London Dry',
            'Aviation American Gin',
            "Gordon's London Dry",
            'Monkey 47 Schwarzwald Dry Gin',
            'Sipsmith London Dry'
        ]
    )
    
    for recipe in recipes:
        expected_abv_volume = recipe.base_volume * (recipe.target_abv_percentage / 100)
        self.assertAlmostEqual(
            recipe.abv_volume,
            expected_abv_volume,
            places=3,
            msg=f"{recipe.name}: abv_volume mismatch"
        )
```

**Verify:**
```bash
python manage.py test tests.test_calculator_views::FamousRecipesCommandTest::test_fixture_abv_volume_is_self_consistent
```

---

### Step 4 — Fix brittle UI test
**File:** `tests/test_ui.py`

**Change:** Replace `test_calculate_formula` with non-numerical version:

```python
def test_calculate_on_input_change(page):
    """Verify calculation pipeline works: input → API call → JS → DOM update."""
    page.goto(BASE_URL)
    page.wait_for_function("document.getElementById('target_abv').value !== ''")

    page.fill("#volume", "1.5")
    page.fill("#input_spirit_abv", "96")
    page.fill("#target_abv", "40")

    # Wait for calculation to complete (results populated, not empty placeholders)
    page.wait_for_function(
        "() => { const v = document.getElementById('spirit-load-value').textContent; return v !== '' && v !== '—'; }",
        timeout=5000,
    )

    # Just verify results are populated (don't assert exact numbers)
    assert page.inner_text("#spirit-load-value") != "—", "Spirit-load value not calculated"
    assert page.inner_text("#water-value") != "—", "Water value not calculated"
```

**Verify:**
```bash
python -m pytest tests/test_ui.py::test_calculate_on_input_change -v
```

---

### Step 5 — Optional: Remove fixture content assertion tests
**File:** `tests/test_calculator_views.py`

**Change:** Delete these two test methods from `FamousRecipesCommandTest`:
- `test_recipes_have_description_notes` (lines 244–251)
- `test_ingredients_have_variation_notes` (lines 253–260)

These break whenever fixture data changes — low signal, high maintenance. Keep only if you want to enforce that every recipe has a description mentioning "distill".

**Verify:**
```bash
python manage.py test tests.test_calculator_views
```

---

### Step 6 — Run full test suite
```bash
python manage.py test calculator
python -m pytest tests/
```

Expect all tests to pass.

---

## Summary of Changes

| File | Change | Reason |
|------|--------|--------|
| `calculator/tests.py` | Delete entirely | Duplicate of `test_calculator_views.py` |
| `tests/test_calculator_views.py:67` | Remove `water_for_maceration` assertion | Field doesn't exist in API response |
| `tests/test_calculator_views.py:71` | Remove from expected_fields list | (same reason) |
| `tests/test_calculator_views.py` | Rename + clarify `test_abv_volume_derived_correctly` | Distinguish fixture consistency check from formula test |
| `tests/test_ui.py` | Replace `test_calculate_formula` with `test_calculate_on_input_change` | Remove brittle number assertion, keep integration check |
| `tests/test_calculator_views.py` | (Optional) Delete fixture content tests | Low signal, breaks on fixture updates |

## What Stays (Good Tests)

- ✓ `test_page_loads` — catches server/template errors
- ✓ `test_input_fields_present` — catches missing inputs
- ✓ `test_recipe_dropdown_populated` — catches empty recipe list
- ✓ `test_target_abv_prefills_from_recipe` — catches JS/API integration issues
- ✓ `test_creates_all_8_recipes` — catches management command breaking
- ✓ `test_idempotent` — catches duplicate recipe creation
- ✓ `test_tanqueray_has_4_ingredients` — catches fixture corruption
- ✓ `test_monkey47_has_47_ingredients` — catches fixture corruption
- ✓ `test_invalid_recipe_id` — catches error handling bugs
- ✓ `test_ingredient_model_relationship` — catches model relationship issues
